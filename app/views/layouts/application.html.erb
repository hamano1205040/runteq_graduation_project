<!DOCTYPE html>
<html>
  <head>
    <title>TrainLog - トレーニング記録アプリ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm">
      <div class="container mx-auto px-4 py-4">
        <div class="flex justify-between items-center">
          <div class="flex items-center space-x-4">
            <%= link_to "TrainLog", root_path, class: "text-2xl font-bold text-blue-600" %>
            <% if user_signed_in? %>
              <%= link_to "トレーニング記録", training_records_path, class: "text-gray-700 hover:text-blue-600" %>
            <% end %>
          </div>
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <span class="text-gray-600"><%= current_user.email %></span>
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded" %>
            <% else %>
              <%= link_to "ログイン", new_user_session_path, class: "text-blue-600 hover:text-blue-800" %>
              <%= link_to "新規登録", new_user_registration_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="container mx-auto px-4 mt-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="container mx-auto px-4 mt-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <main class="container mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>
